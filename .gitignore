__pycache__/
*.pyc
.DS_Store
.env
scratch/
data/
*.zip
*.csv
*.parquet

# but DO track the tiny committed test fixtures (the integration suite reads these)
!tests/fixtures/**/*.csv

# private — not for the public mirror
docs/PROPOSAL.md
STATUS.md
.vercel
site/.vercel

# ZIP/aggregate build artifacts (published to HF, not tracked in git)
aggregates/*.parquet
